fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.5
httpx>=0.27
pytest>=8.0
anthropic>=0.40
pyyaml>=6.0
# Comment-preserving round-trip editing of config/models.yaml for the admin
# UI's git-backed placement writes (#424) — pyyaml stays the fast read path.
ruamel.yaml>=0.18
huggingface_hub>=0.24
psutil>=5.9

# Tray launcher. Pillow underlies pystray's icon images; winotify gives
# WinRT toast fallback on Windows.
pystray>=0.19
Pillow>=10.0
winotify>=1.1; platform_system == "Windows"

# tray/icon.py renders the Lucide hub glyph via resvg-py at runtime — the
# live tray re-renders it on every health-state change (green/amber/grey),
# so unlike project-scaffolding's brand_gen.py callers, this is a real
# runtime dependency, not dev-only.
resvg-py>=0.3.0

# Passkey gate for /admin (issue #7). Optional at runtime — when
# absent, WebAuthnGate.available() returns False and the bearer-token
# alone protects the webapp. Install to enable platform-passkey
# enrollment from the iOS PWA.
webauthn>=2.1

# Gemini backend drives the Antigravity CLI (`agy`) under a ConPTY,
# since `agy -p` print mode renders to a console rather than stdout.
pywinpty>=2.0; platform_system == "Windows"

# Observability — OpenTelemetry + Langfuse (issue #4). The hub emits
# traces + metrics via OTLP/gRPC to a local Langfuse stack
# (docker/langfuse/docker-compose.yml). The langfuse SDK is used only
# by the feedback endpoint + the admin Telemetry tab — never on the
# request hot path.
python-dotenv>=1.0
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-instrumentation-logging>=0.48b0
langfuse>=4.0
