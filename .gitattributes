# Shell scripts must stay LF so they run on the Linux/macOS satellites they
# are deployed to (gaming, mac-mini) — a CRLF shebang yields the classic
# "bad interpreter: /bin/bash^M". Independent of any host's core.autocrlf.
*.sh text eol=lf
