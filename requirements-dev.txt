# Development-only deps — install with `pip install -r requirements-dev.txt`
# after the main requirements.txt. Not shipped by the main install.

# End-to-end browser tests. Chromium-only — WebKit was dropped from the
# matrix because the SPA has no Safari-specific code and WebKit on the
# windows-latest CI runner was chronically flaky (issue #24).
pytest-playwright>=0.5
playwright>=1.45

# Passkey gate is optional in `requirements.txt`; here it's required so
# the webauthn router gets exercised in the e2e gate.
webauthn>=2.1
